/**
 * <<文件说明>>
 *
 * @author    黄文金
 * @copyright Copyright (c) 2014 - 2015, Wedo, Inc. (http://weidu178.com/)
 * @link      http://weidu178.com
 * @since     Version 1.0
 */
namespace {{$ns}};

use Wedo\Database\Entity;

/**
 * <<实体文件说明>>
 */
class {{$entityName}} extends Entity {
@foreach($fields as $f)
    protected ${{$f}};
@endforeach

@foreach($fields as $f)
    /**
     * get ${{$f}}
     *
     * @return mixed
     */
    public function get{{ucfirst($f)}}() {
        return $this->{{$f}};
    }

    /**
     * set ${{$f}}
     *
     * @param string ${{$f}}
     * @param string $adj 条件修饰符
     * @return $this
     */
    public function set{{ucfirst($f)}}(${{$f}}, $adj = NULL) {
        $this->{{$f}} = ${{$f}};
        $this->addCondition('{{$f}}', $adj);
        return $this;
    }
@endforeach

}